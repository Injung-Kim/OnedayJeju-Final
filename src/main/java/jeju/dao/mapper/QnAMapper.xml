<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jeju.dao.face.QnADao">
	<!--질문글 수 조회  -->
	<select id="selectCntQustion" resultType="int">
		SELECT count(*) FROM qa_question
	</select>
	<!-- 질문글 조회 -->
	<select id="selectQuestions" resultType="jeju.dto.QnAQuestion">
		SELECT qst_no, title, user_no, qst_content, qst_time, qst_cnt FROM qa_question
	</select>
</mapper>